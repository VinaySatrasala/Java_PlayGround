public class Maximum69Number {
    public static void main(String[] args) {
        // int num=969;


        // StringBuilder str=new StringBuilder(num+"");
        // if(str.indexOf("6") == -1){
        //     // return num;
        // }

        // Integer.parseInt(str.replace(str.indexOf("6"),str.indexOf("6")+1,"9").toString());

        // System.out.println(str);;
        // String st=num+"";
        // st.replace(str, st);
        // st.replace()
        // st.e
        String s="[]{}";
        // s.replaceAll(s, s)
        s.replaceAll("{"," ");
        s.replace("[]"," ");
        s.replace("()","h");
        System.out.println(s);
        // int pos=str.length()-str.indexOf('6');
        // int res=num+3*(int)Math.pow(10, pos-1);
        // // System.out.println(res);
    }
}
